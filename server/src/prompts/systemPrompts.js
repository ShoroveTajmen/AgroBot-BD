// Legacy prompts kept for reference.
// The new agentic advisoryAgent uses its own inline SYSTEM_PROMPT
// and OpenAI function calling — these are no longer used.

export const getSystemPrompt = () => '';
export const getFollowUpPrompt = () => '';
